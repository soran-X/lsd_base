<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Edit Book</h1>
  <p class="mt-1 text-sm text-gray-500">Update the details for <span class="font-medium text-gray-700"><%= @book.title %></span>.</p>
</div>

<%= render "form", book: @book %>

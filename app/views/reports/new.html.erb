<% content_for :title, "New Report" %>

<div class="mb-6">
  <div class="flex items-center gap-2 text-sm text-gray-500 mb-2">
    <%= link_to "Reports", reports_path, class: "hover:text-gray-700" %>
    <span>/</span>
    <span class="text-gray-900">New</span>
  </div>
  <h1 class="text-2xl font-bold text-gray-900">New Report</h1>
</div>

<%= render "reports/form", report: @report, preloaded_books: @preloaded_books %>

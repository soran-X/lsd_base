<%# ── Header ─────────────────────────────────────────────────────────────────── %>
<div class="flex items-center justify-between mb-6">
  <div>
    <h1 class="text-2xl font-bold text-gray-900">Genres</h1>
    <p class="text-sm text-gray-500 mt-0.5">Manage genres used to categorise books.</p>
  </div>
  <%= link_to "New Genre", new_genre_path,
        class: "inline-flex items-center gap-1.5 px-4 py-2 bg-indigo-600 text-white text-sm
                font-medium rounded-lg hover:bg-indigo-700 transition-colors" %>
</div>

<%# ── Genre list ─────────────────────────────────────────────────────────────── %>
<div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
  <% if @genres.any? %>
    <ul class="divide-y divide-gray-100">
      <% @genres.each do |genre| %>
        <li>
          <div class="flex items-center justify-between px-5 py-3 hover:bg-gray-50 group">
            <span class="text-sm font-medium text-gray-800"><%= genre.name %></span>
            <div class="flex items-center gap-3 opacity-0 group-hover:opacity-100 transition-opacity">
              <%= link_to "Edit", edit_genre_path(genre),
                    class: "text-xs text-gray-500 hover:text-gray-700 font-medium" %>
              <%= button_to "Delete", genre_path(genre), method: :delete,
                    data: { turbo_confirm: "Delete \"#{genre.name}\"?" },
                    class: "text-xs text-red-500 hover:text-red-700 font-medium bg-transparent border-0 cursor-pointer p-0" %>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <div class="text-center py-16 text-sm text-gray-400">
      <p class="mb-3">No genres yet.</p>
      <%= link_to "Add the first genre", new_genre_path,
            class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
    </div>
  <% end %>
</div>
